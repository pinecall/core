# Your Pinecall API key
PINECALL_API_KEY=pk_...

# The slug this agent registers as
AGENT=outbound-dispatch

# The number to call FROM (E.164) — required, this example dials out
PHONE=+15550001111

# Where the leads live; the same file receives the results
LEADS=./data/leads.csv
