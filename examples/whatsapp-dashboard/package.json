{
  "name": "pinecall-whatsapp-dashboard-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev": "node --env-file=.env --watch server.mjs",
    "start": "node --env-file=.env server.mjs"
  },
  "dependencies": {
    "@pinecall/sdk": "file:../../",
    "express": "^5.1.0"
  }
}
